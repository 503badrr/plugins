# Changelog

All notable changes to this plugin will be documented here.

## 1.0.0 — initial release

- Added the `activecampaign` MCP server, pointed at a user-supplied Remote MCP URL.
- Auth is OAuth against ActiveCampaign — no API key to configure.
- Logo: ActiveCampaign's official mark, from the `ActiveCampaign` GitHub organization.
